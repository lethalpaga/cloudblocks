require 'spec_helper'

describe CloudBricks do
  it 'has a version number' do
    expect(CloudBricks::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
